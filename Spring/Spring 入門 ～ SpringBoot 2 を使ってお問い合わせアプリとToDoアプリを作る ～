# Entityクラス
```java
public class Member {
  private int id; // field メンバ変数
  private String name; // field メンバ変数
  private String email; // field メンバ変数

  // constructor メソッド、メンバ変数とも言う
  // getter and setter
  }
  ```

# インターフェース
- 機能の概要を記す
- クラスの自動生成をすることがある（MyBatisなどで）

- Cmd + Shift + O -> import読み込み

# シングルトン
クラスのインスタンスが必ず一つであることを保証するデザインパターンのこと。

- クラスをインスタンス化する際はクラスオブジェクトが必ず一つ作られる。
- 普通はクラスオブジェクト一つに対して複数のインスタンスオブジェクトができる。
-

# デザインパターン
オブジェクト指向において様々なプログラムで再利用できる設計パターン

# Gradleとは
ビルドツールの一つ。ソースコードのコンパイルテストを実行してレポートの出力、クラスファイルのアーカイブ生成、アーカイブをステージング環境にリリースするなどの定型的な作業を自動化するソフトウェア。

Mavenも同じようなもの

# アノテーション
- @をつけたコード
- @Controllerをつけると必要なときに自動的にインスタンス化される

# Dependency Injection (DI)
インスタンス化されたオブジェクトを自動で渡してくれる機能。裏でDIコンテナというクラスが働いている。

# ステートレス
システムが現在の状態を表すデータを保持しないこと。基本的にはアクセスごとにデータは失われる
